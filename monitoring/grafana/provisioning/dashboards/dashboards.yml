apiVersion: 1

providers:
  - name: 'DCA-Auth Dashboards'
    orgId: 1
    folder: 'DCA-Auth'
    folderUid: 'dca-auth'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json